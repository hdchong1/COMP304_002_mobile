<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hyongdonjeong_juheekim_comp304lab4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GitTestActivity"></activity>
        <activity android:name=".UpdatePatientActivity" />
        <activity android:name=".PatientInfoActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".DatabaseExActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>